# NV12 CPU Crop 极致性能优化

> 由 autoresearch-init 自动生成 | 2026-04-08

## 研究目标

对 NV12 格式图像的 crop 操作进行极致性能优化，目标是接近内存带宽极限。

**核心指标**：avg_ns/op，越低越好，目标：比朴素 memcpy 实现降低 30% 以上延迟（或带宽利用率达到理论值 60%+）

## 评估方法

| 步骤 | 命令 | 说明 |
|------|------|------|
| 构建 | `make clean && make` | 编译项目和测试 |
| 评估 | `./bench` | 采集性能数据（1000次迭代取平均） |
| 测试 | `./test_correctness` | 正确性验证 |
| 稳定性 | 无 | 不需要长时间稳定性测试 |

**指标解析**：从 bench 输出中提取 `avg_ns/op` 字段。同时关注 `bandwidth_GB/s` 作为辅助指标。

## 文件规则

| 文件 | 可修改？ | 说明 |
|------|---------|------|
| nv12_crop.c | ✅ 可修改 | 优化目标文件 |
| nv12_crop.h | ⚠️ 谨慎修改 | 仅允许添加内部函数声明，不得改变公开 API |
| bench.c | ❌ 不可修改 | 评估基准 |
| test_correctness.c | ❌ 不可修改 | 正确性标准 |
| Makefile | ⚠️ 谨慎修改 | 仅允许修改编译选项和 CFLAGS |

## QA 门禁

每次实验**必须**通过以下检查才能保留：

1. `make clean && make` 编译通过（无 warning）
2. `./test_correctness` 通过（Y + UV 全像素验证）
3. 核心指标相对当前最优**不退化**（在约定噪声范围内视为持平）：avg_ns/op 越低越好，新结果不得劣于当前最优

## 迭代参数

- **最大迭代次数**：10
- **提前结束条件**：连续 3 次无改善

## 实验记录

| # | 实验 | 结果 | 决策 | 指标值 |
|---|------|------|------|--------|
| 1 | AVX2 SIMD | 54176.7 ns | ✅ | 改善 4.2% |

## 待探索方向

- 循环展开（减少分支开销）
- 非临时存储指令 (_mm256_stream_si256)
- 预取指令 (_mm_prefetch)
- Y/UV 平面并行处理（需要双缓冲）
- 更激进的对齐优化

## 当前最优

- **指标值 (avg_ns/op)**：54176.7 ns
- **带宽**：23.76 GB/s
- **commit**：d7f34a7
- **改善幅度**：相比原始 memcpy 基准 (56566.2 ns) 降低 4.2%
